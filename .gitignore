# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# uv
uv.lock.bak

# Kiwi workspaces: derived index/cache, never source of truth
.kiwi/

# Eval corpus ingestion output, regenerate with:
#   kiwi ingest eval/corpus --project eval/workspace.kiwi
# The frozen inputs (eval/corpus/*.pdf, eval/golden.json) are committed;
# this derived workspace is not.
eval/workspace.kiwi/

# Docs
/docs/

# Logs
*.log

# Environment variables
.env
.env.*
!.env.example

# Interface test project, rebuild with:
#   uv run python tests/frontend/fixture.py
Test.kiwi/

# Field test PDFs and project, not part of the repository
fieldtest/
FieldTest.kiwi/

# Held-out eval workspace, regenerate with:
#   kiwi ingest eval/corpus-heldout --project eval/heldout.kiwi
eval/heldout.kiwi/

# SciFact benchmark data and index, fetch with:
#   uv run python eval/_scifact.py --download
eval/scifact/
eval/scifact-index/
